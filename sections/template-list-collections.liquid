<div class="flex flex-col items-center my-8">
  <h1 class="text-3xl font-medium">{{ section.settings.title }}</h1>

  <div class="max-w-6xl mx-auto my-3">
    <div class="grid grid-cols-1 md:grid-cols-3 gap-3">
      {% for collection in collections %}
        {% if collection.all_products_count > 0 %}
          <div class="w-full py-6 px-3">
            <div class="bg-white rounded-2xl overflow-hidden">
              <a href="{{ collection.url }}">
                <div class="h-80 overflow-hidden">
                  {% if collection.featured_image != blank %}
                    {% assign collection_image = collection.featured_image %}

                  {% else %}
                    {% assign collection_image = collection.products.first.image %}
                  {% endif %}
                  <img
                    class="w-full h-full object-cover transition duration-1000 hover:scale-110 ease-out transform"
                    src="{{ collection_image | image_url: height: 200, width: 200 }}"
                    alt="collection.title | ease"
                    height="200"
                    width="200"
                  >
                </div>
              </a>

              <div class="p-4 bg-gray-100">
                <h1 class="text-xl">{{ collection.title }}</h1>
              </div>
            </div>
          </div>
        {% endif %}
      {% endfor %}
    </div>
  </div>
</div>

{% schema %}
{
  "name": "Template List Collection",
  "settings": [
    {
      "type": "text",
      "id": "title",
      "default": "collections",
      "label": "Title"
    }
  ]
}
{% endschema %}
